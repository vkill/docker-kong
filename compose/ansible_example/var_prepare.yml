---
dotenv_dir: "{{ playbook_dir }}"

kong_log_dir: "/tmp/kong_log"

kong_postgres_password: "kong"
kong_postgres_data_dir: "/tmp/kong_postgres_data"
kong_postgres_log_dir: "/tmp/kong_postgres_log"

kong_dashboard_password: "kong"
